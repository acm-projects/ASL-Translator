import 'package:flutter/material.dart';

class AbtUs extends StatelessWidget
{
  @override
  Widget build(BuildContext context)
  {
    return Container(
      padding: EdgeInsets.all(25.0),
      child: Text('About Us', style: TextStyle(fontSize: 36.0),),
    );
  }
}